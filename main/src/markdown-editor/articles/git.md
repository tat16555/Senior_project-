Quick setup — if you’ve done this kind of thing before
https://github.com/tat16555/E-Commerce-tpsad-cf.git
Get started by creating a new file or uploading an existing file. We recommend every repository include a README, LICENSE, and .gitignore.

…or create a new repository on the command line
echo "# E-Commerce-tpsad-cf" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M add-ec
git remote add origin https://github.com/tat16555/E-Commerce-tpsad-cf.git
git push -u origin add-ec


…or push an existing repository from the command line
git remote add origin https://github.com/tat16555/E-Commerce-tpsad-cf.git
git branch -M add
git push -u origin add
  git config --global user.email "pannatat.b@gmail.com"
  git config --global user.name "tat16555"
  git remote rename origin <add-ec>
  git remote set-url origin https://github.com/tat16555/E-Commerce-tpsad-cf.git


